import { View, Text, SafeAreaView } from "react-native";
import AppBar from "../components/AppBar";

const HomeScreen = () => {
  return (
    <SafeAreaView>
      {/* <AppBar /> */}
      <View>
        <Text className=" text-red-600">HoomeScreen</Text>
      </View>
    </SafeAreaView>
  );
};

export default HomeScreen;
